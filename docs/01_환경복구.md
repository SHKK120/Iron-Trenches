# 환경 복구

군 싸지방처럼 세션 종료 또는 재부팅 뒤 PC가 초기화되는 환경에서, 로컬 상태에 의존하지 않고 저장소만으로 개발을 다시 시작하기 위한 Recovery Runbook이다.

## 문서의 역할

프로젝트를 시작하거나 초기화 PC에서 개발 환경을 복구할 때 필요한 실제 확인값과 운영 절차를 관리한다. 계획값을 사실처럼 고정하지 않고, 확인된 환경값을 누적한다.

## 이 문서가 소유하는 정보

- Repository / Clone과 실제 프로젝트 경로
- 엔진, Package와 로컬 도구의 정확 버전
- MCP 및 `.codex` 로컬 설정 복구
- Project Skill 복구
- 프로젝트 실행, 기계적 Smoke와 원격 Checkpoint 확인
- 초기화 PC 재개 절차와 보안 경계

## 이 문서가 소유하지 않는 정보

- 게임 플레이 규칙과 실제 전투 수치
- 현재 구현 순서와 다음 행동
- 사람의 재미 판정

## 복구 모델

- 로컬 디스크, `.git`, Git credential, Codex 개인 설정과 도구 설치는 언제든 사라질 수 있다고 가정한다.
- 복구 가능한 작업의 기준은 승인된 원격 경로에 남은 마지막 검증 Checkpoint다.
- 작업 종료 전에는 복구에 필요한 코드, 문서, 버전 고정 파일과 스크립트를 Checkpoint에 포함한다.
- 커밋되지 않았거나 push되지 않은 작업은 PC 초기화 시 복구할 수 없다.
- 인증정보는 복구 대상이 아니다. 매 세션 안전하게 다시 인증하고 로컬 또는 저장소에 남기지 않는다.

## Repository

- Repository URL: `[확인 사실] https://github.com/SHKK120/Iron-Trenches`
- 공개 상태: `[확인 사실] Public`
- 기본 branch: `[확인 사실] main`
- 마지막 환경복구 원격 HEAD: `[확인 사실 — 2026-09-09] 87b5482a3cb44647b696975572fb986a9a1d2db9`
- GAME-DIRECTION-R1: `[원격 보존] main의 GAME-DIRECTION-R1 Checkpoint`; 정확한 최신 HEAD는 복구 시 Git에서 조회
- 원격 HEAD 정본: 복구 시 GitHub `main`에서 다시 조회한 최신 commit
- 과거 임시 Codex workspace: `[과거 확인] C:\Users\CKIRUser\Downloads\Iron-Trenches-main`
- 과거 ZIP 안전본 source: `[과거 확인] C:\Users\CKIRUser\Downloads\Iron-Trenches-main\Iron-Trenches-main`
- 고정 작업 root: `[복구 규칙] %USERPROFILE%\Iron-Trenches`
- 현재 PC 고정 작업 root: `[확인 사실 — 해당 PC] C:\Users\CKIRUser\Iron-Trenches`
- 고정 작업 root 상태: `[확인 사실 — 2026-09-09] 정상 Clone 완료, origin/main 및 HEAD 87b5482a3cb44647b696975572fb986a9a1d2db9 확인`
- 현재 작업 폴더 형태: `[확인 사실] C:\Users\CKIRUser\Iron-Trenches의 정상 Git working copy`
- 정상 Clone 뒤 remote 이름: `origin`

## 정상 Clone과 작업 폴더

새 PC에서는 다운로드 ZIP을 안전본으로 먼저 확인하고, GitHub Repository에서 정상 Clone한 `%USERPROFILE%\Iron-Trenches`를 유일한 작업 폴더로 사용한 뒤 그 Clone root를 Codex에서 연다.

```text
새 PC
→ 다운로드 ZIP을 비교용 안전본으로 확인
→ GitHub Repository에서 정상 Clone
→ %USERPROFILE%\Iron-Trenches를 프로젝트 작업 폴더로 사용
→ 그 root를 Codex에서 Open
```

일반 폴더를 만든 뒤 Windows 복사로 파일을 넣고 나중에 Git을 연결하는 방식은 정상 복구로 사용하지 않는다. 다운로드 ZIP과 `Iron-Trenches-main` 형태의 폴더는 Git working copy가 아니라 임시 반입/안전본으로 구분한다. ZIP 폴더에서 개발하거나 Unity 프로젝트를 열지 않는다.

### ZIP 확인과 Clone 전환

1. ZIP은 `Downloads` 아래에서만 풀고 파일 목록, 문서, 예상하지 못한 변경과 Secret 후보를 읽기 전용으로 확인한다.
2. ZIP 안의 실행 파일, 스크립트, Unity 프로젝트와 설치 프로그램을 복구 원본으로 실행하지 않는다.
3. `%USERPROFILE%\Iron-Trenches`가 없을 때만 그 정확한 경로를 Clone Target으로 사용한다.
4. Target이 이미 있으면 삭제·덮어쓰기·재Clone하지 않는다. `.git`, `origin`, branch와 변경 상태를 확인하고 충돌 시 작업을 중단한다.
5. 정상 Clone 뒤 ZIP과 Clone을 비교한다. ZIP에만 있는 변경은 파일별로 검토하고 현재 Result Bundle에 필요한 것만 Clone에 반영한다.
6. `.git`, `Library`, `Temp`, `Logs`, `obj`, IDE 상태, `.codex`, 인증정보와 Secret은 ZIP에서 Clone으로 복사하지 않는다.
7. Codex 연결 폴더를 `%USERPROFILE%\Iron-Trenches`로 바꾸고, 이후 읽기·수정·Unity Open·검증·Commit·Push는 Clone root에서만 수행한다.

ZIP 확인은 원격 Clone을 대체하지 않는다. ZIP과 원격이 같아도 `.git`이 없으면 정상 작업환경이 아니다.

Git CLI 사용이 허용된 경우 다음처럼 고정 작업 root가 아직 없을 때만 Clone한다.

```powershell
git clone https://github.com/SHKK120/Iron-Trenches.git "$env:USERPROFILE\Iron-Trenches"
Set-Location -LiteralPath "$env:USERPROFILE\Iron-Trenches"
.\scripts\restore-session.ps1 -Online
```

기존 변경이 있으면 보존하고 원인을 확인한다. 복구를 이유로 `git reset --hard` 또는 `git clean`을 사용하지 않는다.

고정 Clone 대상 폴더가 이미 있으면 덮어쓰거나 삭제하거나 다른 영구 작업 root를 만들지 않는다. 기존 폴더를 보존한 채 `.git`, remote, branch와 로컬 변경을 검사하고, 정상 working copy가 아니면 사용자 판단 없이 이동·교체하지 않는다.

## 관리형 PC Git fallback

우선순위는 다음과 같다.

1. Codex를 `%USERPROFILE%\Iron-Trenches` Clone root로 열고 Codex 내장 명령 실행으로 정상 Git workflow 수행
2. Codex 명령 실행만 정책으로 차단되면 Codex가 검토된 Git Bash 명령 블록을 제공하고 사용자가 Git Bash에서 한 번 실행
3. 관리형 PC에서 이미 허용된 Git GUI 또는 GitHub Desktop
4. Codex GitHub App / Connector `[읽기·원격 확인 편의 기능, Clone working copy를 대체하지 않음]`

- Codex가 ZIP workspace에 연결된 상태에서는 Git 작업을 시작하지 않는다. 먼저 작업 폴더를 `%USERPROFILE%\Iron-Trenches`로 다시 연다.
- Codex 내장 명령 실행에서 Git CLI가 가능하면 Codex가 status, 명시적 stage, commit과 승인된 push를 직접 수행한다. 화면에 열린 Git Bash 창을 Codex가 타이핑하거나 자동 조작하지 않는다.
- Codex 명령 실행이 `CreateProcessAsUserW failed`, `Access Denied` 등으로 차단되면 반복하지 않는다. Codex는 대상 파일이 명시된 한 개의 Git Bash 명령 블록을 제공하고, 사용자가 실행한 뒤 출력 또는 원격 HEAD를 Codex가 확인한다.
- 관리자 권한 상승, Execution Policy 완화, 다른 binary를 이용한 정책 우회와 보안 기능 비활성화를 금지한다.
- 이미 허용된 Git GUI 또는 정상 GitHub 경로가 있으면 이를 사용한다.
- Git GUI를 사용할 때는 `Clone Existing Repository`에서 Source를 `https://github.com/SHKK120/Iron-Trenches.git`, Target을 `%USERPROFILE%\Iron-Trenches`로 지정한다. ZIP 폴더를 Target으로 재사용하지 않는다.
- Codex GitHub App이 read-only이거나 write 403이어도 정상 Git/GUI가 있으면 프로젝트 개발 자체의 blocker로 간주하지 않는다.
- 모든 원격 쓰기 경로가 불가능하면 새 기능보다 현재 작업의 로컬 보존을 우선하고 `[원격 미보존]`을 기록한다.
- PC 초기화가 임박했고 원격 생존본이 없다면 새 기능보다 허용된 원격 보존을 우선한다.

## 현재 Git / GitHub 연결 상태

- Git working copy: `[확인 사실 — 해당 PC] C:\Users\CKIRUser\Iron-Trenches에 정상 Clone 존재`
- 현재 Codex 연결: `[확인 사실 — 2026-09-09] C:\Users\CKIRUser\Iron-Trenches Clone root`
- Codex Git 실행: `[확인 사실 — 2026-09-09] status, 명시적 stage, commit과 origin/main push 성공`
- 일반 PowerShell 실행: `[BLOCKED — 관리형 PC 정책, Access denied 확인]`
- Git GUI: `[확인 사실 — 해당 PC] C:\Program Files\Git\cmd\git-gui.exe 존재`
- Git GUI Clone: `[확인 사실] 고정 작업 root에 성공`
- Git GUI Stage: `[확인 사실] 네 파일 모두 성공`; `Index Error` 창의 실제 내용은 LF가 다음 Git 처리 시 CRLF로 바뀐다는 경고였으며 실패가 아니다.
- Git GUI Commit: `[확인 사실] 초기화 PC의 작성자 정보 누락을 저장소 로컬 `.git/config`에 복구한 뒤 성공`
- GitHub App 읽기: `[확인 사실] 가능`
- GitHub App 권한: `[확인 사실 — 2026-09-09] pull / push 가능 표시, Contents 쓰기는 403 Resource not accessible by integration`
- GitHub App 위치: 복구 필수조건이 아닌 선택 편의 도구
- 최신 로컬 FOUNDATION-R1: `[확인 사실] 문서 최신화 직전 원격 main tree와 일치`
- DEV-BOOT 도구 복구 기준선: `[원격 보존]` — commit `a90224876372e2f8afddd8815303815391885c50`
- Unity 프로젝트 기준선 추가분: `[원격 보존]` — commit `e1bcf0f25d3ffd2d166f19b883799b423c89d919`
- Codex Git workflow 기준선: `[원격 보존]` — commit `87b5482a3cb44647b696975572fb986a9a1d2db9`
- Managed PC Source-Only 개발 기준선: `[원격 보존] main의 Managed PC Source-Only Development Checkpoint`

초기화 PC에서 Git GUI Commit이 `Committer identity unknown`으로 중단되면 해당 저장소에만 기존 프로젝트 작성자 이름과 이메일을 설정한다. 전역 설정을 만들거나 임의의 신원을 사용하지 않는다. LF→CRLF 메시지만 표시되고 파일이 `Staged Changes`에 있으면 Stage는 성공한 것이므로 `Unlock Index`를 누르지 않고 경고를 닫아 계속한다.

## 매 세션 전제 조건

1. ZIP 안전본을 확인한 뒤 `%USERPROFILE%\Iron-Trenches`에 정상 Git working copy를 확보한다.
2. GitHub 웹에서 `SHKK120/Iron-Trenches` 접근을 확인한다.
3. public 저장소 Clone에는 토큰을 파일에 저장하지 않는다.
4. push가 필요하면 해당 세션에서만 승인된 GitHub 인증을 사용한다.
5. 설치 권한이나 네트워크 정책 때문에 선택한 경로를 사용할 수 없으면 우회하지 않고 상태와 대체 경로를 기록한다.
6. Checkpoint는 Codex 내장 Git을 우선하고, 차단 시에만 사용자가 Codex가 제공한 명령 블록을 Git Bash에서 실행한다.
7. Push 뒤 Codex가 GitHub `main`의 최신 commit과 메시지를 확인한다.

Git이 없다면 시설 정책이 허용하는 공식 설치 경로를 사용한다. 관리자 권한 우회, 보안 기능 비활성화, 출처 불명 portable 실행 파일은 사용하지 않는다.

## 환경별 복구 모드

같은 원격 Checkpoint를 두 환경에서 사용하며 코드와 문서를 별도 복사본으로 분기하지 않는다.

### ManagedPc — 관리형 PC 기본값

- 매 세션 필수: 정상 Git Clone/Pull, Codex, 승인된 Git 실행 경로, 브라우저
- Source 검증: `global.json`이 고정한 .NET SDK 8.0.318과 `tools/ManagedPcChecks`
- 사람 검증: 외부 패키지와 서버가 없는 `Preview/index.html`
- Unity Hub, Unity Editor, Visual Studio, Unity Package Import와 MCP는 설치·실행 필수조건이 아니다.
- 이미 설치된 Unity 내장 .NET SDK는 ManagedPcChecks에 사용할 수 있지만, SDK만을 위해 Unity를 다시 설치하지 않는다.
- 공식 .NET SDK를 사용할 수 없는 세션은 C# Bundle을 PASS 처리하지 않고 Browser Preview와 문서 작업만 진행한 뒤 `[Source Check BLOCKED]`를 기록한다.

기본 검사 명령은 `scripts/restore-session.ps1 -Mode ManagedPc -Online`이다. PowerShell 실행이 정책으로 막히면 같은 항목을 수동 확인하며 우회하지 않는다.

### UnityIntegration — 개인 PC 전환

1. 관리형 PC에서 push한 것과 같은 `origin/main` Checkpoint를 정상 Clone/Pull한다.
2. `ManagedPcChecks`와 `Preview/index.html`로 Source-Only 기준선이 같은지 먼저 확인한다.
3. `scripts/restore-session.ps1 -Mode UnityIntegration -Online`으로 Unity 통합 요구사항을 확인한다.
4. 프로젝트가 고정한 Unity 6000.6.0f1과 필요한 모듈을 공식 경로로 설치한다.
5. Compile / Scene / Play / Reopen / MCP 검증을 추가하고 `Unity Integration`을 별도 판정한다.

`Library/`, `Temp/`, `Logs/`, IDE 상태와 인증정보는 PC 사이에서 옮기지 않는다. 개인 PC는 같은 Source Checkpoint에서 Unity 생성물을 다시 만들며, 관리형 PC의 Browser Preview PASS를 Unity PASS로 승격하지 않는다.

## Engine

- Unity Hub 정확 버전: `[확인 사실] 43.5.0`
- Unity Hub 설치 위치: `[확인 사실 — 해당 PC] C:\Program Files\Unity Hub\Unity Hub.exe`
- Unity Editor 정확 버전: `[확인 사실] 6000.6.0f1`
- Unity Editor 설치 위치: `[확인 사실 — 해당 PC] C:\Program Files\Unity\Hub\Editor\6000.6.0f1\Editor\Unity.exe`
- 설치 모듈: `[확인 사실] Windows Standalone Support, WebGL Build Support`
- 미설치 모듈: `[확인 사실] Android Build Support`
- Unity 프로젝트 root: `[확인 사실 — 해당 PC] C:\Users\CKIRUser\Iron-Trenches\Game`
- Project Open 방법: Unity Hub의 프로젝트 목록에서 `Game` Open
- Render Pipeline: `[확인 사실] URP 17.6.0`
- Input 관련 Package: `[확인 사실] Input System 1.20.0`
- Visual Studio Editor Package: `[확인 사실] com.unity.ide.visualstudio 2.0.26`
- External Script Editor: `[미확인]` Visual Studio package와 workload 설치는 확인했지만 Unity Preferences의 실제 선택값은 확인하지 못함
- Entities / DOTS 설치 여부: `[미구성]`
- Main Dev Scene: `[확인 사실] Game/Assets/Scenes/SampleScene.unity`
- Project Open / Scene Load: `[확인 사실 — 2026-09-09 재확인] Editor 실행과 SampleScene Load 성공`
- Compile 확인법: Editor Console의 Error 수와 `Game/Logs/Editor.log`의 Bee 결과를 함께 확인
- Compile: `[BLOCKED — 2026-09-09 재확인] bee_backend가 cmd.exe를 통한 ApiUpdater 실행에 실패, GetLastError 5; Safe Mode 진입`
- Console: `[오류 있음] Bee build failure와 Unity Services Project ID 401/400 확인`; C# `error CS`와 Missing Script 로그는 확인되지 않음
- 최소 Play Smoke: `[미실행] Compile Error 0이 아니므로 PASS 금지`
- Editor 재실행 Smoke: `[미실행]`

## 현재 관리형 PC Compile Blocker

`[확인 사실 — 해당 PC]`

```text
cmd.exe 직접 실행
→ Access Denied

Unity Editor Compile / Import
→ Bee가 ApiUpdater 실행 시도
→ cmd.exe 프로세스 생성 필요
→ GetLastError 5 / Access Denied
→ Compile 완료 불가
→ Safe Mode 진입
```

- 관리자 실행뿐 아니라 일반 사용자로 다시 실행한 경우에도 Standard / Normal Mode의 정상 Compile 기준선을 만들지 못했다.
- `[사용자 확인 — 해당 PC]` Safe Mode에서 나오는 것 자체는 가능하다. 그러나 Bee / ApiUpdater Compile 성공이나 Play Baseline을 뜻하지 않는다.
- 따라서 문제를 관리자 실행 하나로 설명하지 않는다.
- C# `error CS`는 확인되지 않았으며 프로젝트 코드 오류가 원인이라고 확정할 근거가 없다.
- 판정: `DEV-BOOT Core = BLOCKED`; Blocker는 관리형 PC의 프로세스 실행 정책이다.
- Q0: `PARTIAL`; Unity MCP는 미구성, Recovery Skill은 미생성이다.

첫 임시 URP Blank 시험은 Burst compiler server의 `\\.\pipe\unity-burst-*` 생성 실패로 중단됐다. 이후 정상 Clone 안의 `Game` 프로젝트는 생성, Project Open과 SampleScene Load까지 성공했지만 `bee_backend`가 `cmd.exe /c`를 통해 Unity 내장 `dotnet.exe`와 ApiUpdater를 실행하는 단계에서 Windows `GetLastError 5 (Access denied)`로 실패했다. Hub의 총 RAM 4GB 미만 경고도 표시됐다.

이 PC에서는 Unity / Hub / Visual Studio 재설치, 프로젝트 재생성, `Library/` 삭제, Package 전면 재설치, Unity 버전 변경, Safe Mode 강제 Exit, 관리자 실행 반복, 다른 shell 대체, Execution Policy / Registry / AppLocker / 보안 정책 변경과 출처 불명 binary 사용을 더 시도하지 않는다. 정책을 우회해 Compile을 통과시키지 않는다.

## 관리형 PC Source-Only 개발 경로

`[확인 사실 — 해당 PC]` Unity 내장 .NET SDK 8.0.318과 Visual Studio MSBuild 18.10은 shell을 거치지 않은 직접 프로세스 실행이 가능하다. 저장소의 `global.json`은 Source-Only 기준 SDK를 8.0.318로 고정한다. `tools/ManagedPcChecks/ManagedPcChecks.csproj`에서 Unity API에 의존하지 않는 `Game/Assets/Scripts/Core/**/*.cs`를 별도로 컴파일하고 도메인 검사를 실행할 수 있다.

이 경로는 보안 우회가 아니라 허용된 compiler 프로세스를 직접 사용하는 제한된 개발 Lane이다.

### 이 PC에서 허용하는 작업

- `UnityEngine`, Scene, Prefab과 Component에 의존하지 않는 순수 C# Core 작성
- Command, Territory, Economy, Supply 등 deterministic domain rule 작성
- `ManagedPcChecks`의 무외부패키지 assertion으로 규칙 검증
- `Preview/index.html`을 직접 열어 같은 시나리오의 조작·가독성·플레이 감각 확인
- 문서, 데이터 계약과 Unity adapter 경계 설계
- 정적 검토와 Git Checkpoint

### 이 PC에서 완료 처리하지 않는 작업

- Unity Editor Compile / Import 성공
- MonoBehaviour / Component 연결
- Scene / Prefab serialization
- Play Mode, 화면, 입력, 물리, NavMesh와 렌더링
- Missing Script / Missing Reference 최종 확인
- Unity MCP Smoke

각 코드 Bundle은 `Source-Only PASS`와 `Unity Integration DEFERRED`를 따로 기록한다. 정상 PC에서는 Source-Only Checkpoint부터 Unity 6000.6.0f1로 Compile / Play / Reopen 검증한 뒤 통합 완료를 판정한다.

Browser Preview는 C# 실행기가 아니다. 순수 C# Core와 ManagedPcChecks가 기술 정본이며 Preview는 같은 시작 상태·조작·기대 결과를 사람이 확인하는 표현 계층이다. Preview에만 새 규칙을 만들거나 Browser PASS를 Source-Only PASS로 대체하지 않는다.

현재 PC에서 확인한 직접 실행 규격:

```text
Executable:
C:\Program Files\Unity\Hub\Editor\6000.6.0f1\Editor\Data\DotNetSdk\dotnet.exe

Arguments:
build tools/ManagedPcChecks/ManagedPcChecks.csproj --configuration Release --output Game/Temp/ManagedPcChecks --nologo

Then run:
Game/Temp/ManagedPcChecks/ManagedPcChecks.dll
```

실제 결과: build 경고 0 / 오류 0, `PASS ManagedPcChecks (36 assertions)`.

현재 프로젝트 Baseline은 Unity `6000.6.0f1`이다. 다음 정상 PC에서도 먼저 이 정확한 버전으로 Compile을 확인한다. Upgrade / Downgrade는 실제 기술 문제가 확인된 경우 별도 Result Bundle로만 수행한다.

## Unity 프로젝트 구조

현재 정본 구조는 다음과 같다. Repository root 자체를 Unity root로 바꾸거나 `Game/`을 이동하지 않는다.

```text
Iron-Trenches/
├─ AGENTS.md
├─ docs/
├─ scripts/
└─ Game/
   ├─ Assets/
   ├─ Packages/
   └─ ProjectSettings/
```

`[과거 확인]` 과거 Iron & Trenches 자료에는 Unity / DOTS 기술 방향이 기록되어 있었다. 이는 현재 설치 상태나 적용 시점의 확정이 아니다.

Unity 6.6은 현재 선택한 Supported release다. 지원 상태와 최신 patch는 변할 수 있으므로 새 버전으로 자동 변경하지 말고, 복구 시 공식 지원 상태를 확인한 뒤 프로젝트의 `ProjectSettings/ProjectVersion.txt`가 요구하는 정확 버전을 설치한다.

Unity Hub와 Unity Editor는 항상 일반 사용자 권한으로 실행한다. `Administrator Privileges Detected`가 표시되면 `Ignore Warning`을 선택하지 않고 `Restart as Standard User`를 선택한다. 같은 경고가 다시 표시되면 Editor만 반복 재시작하지 않는다. Hub 창을 닫는 것만으로는 백그라운드 Hub 프로세스가 남을 수 있으므로, 알림 영역의 Unity Hub 아이콘에서 Hub를 완전히 종료한 뒤 바탕 화면 또는 시작 메뉴에서 `관리자 권한으로 실행`이 아닌 일반 실행으로 다시 연다. 바로가기와 실행 파일의 호환성 설정에 `관리자 권한으로 이 프로그램 실행`이 켜져 있다면 사람이 승인된 PC 설정 절차로 해제하며, 보안 설정이나 UAC를 우회·비활성화하지 않는다.

Hub를 완전히 종료·재실행한 뒤에도 경고가 반복되고 바로가기의 관리자 실행 플래그가 꺼져 있으며 실행 파일 manifest가 `asInvoker`라면, 현재 Windows 로그인 세션 또는 시설 정책이 관리자 토큰을 부여한 상태로 판정한다. 이때 재시작 버튼을 반복하거나 UAC·보안 정책을 변경하지 않는다. 작업을 중단하고 PC 재부팅 뒤 일반 실행을 다시 확인하며, 그래도 반복되면 표준 사용자 계정 사용 또는 시설 관리자 지원이 필요하다고 기록한다. 관리자 경고를 무시한 실행은 자동 복구 절차에 포함하지 않는다.

사용자가 명시적으로 현재 세션의 진단 실행을 승인해 경고를 넘긴 경우에도 이는 복구 기본 절차로 승격하지 않는다. 해당 실행은 프로젝트 Open과 Compile 가능 여부만 확인하고, 보조 프로세스·named pipe·Compile 오류가 발생하면 즉시 종료해 실제 실패 원인을 기록한다.

## 개인 PC Unity 통합 도구 설치 순서

이 절차는 `UnityIntegration` 모드에서만 적용한다. 관리형 PC의 매 세션 복구에는 적용하지 않는다. 도구 설치는 다음 순서로 한 번씩만 수행한다.

1. 정상 Clone 또는 승인된 원격 작업 환경을 먼저 확보한다.
2. Unity 공식 배포 경로에서 Unity Hub를 기본 경로에 설치한다.
3. Hub에서 Unity ID 로그인과 License 활성화를 사람이 완료한다. 인증정보는 문서나 저장소에 남기지 않는다.
4. Hub의 `Installs`에서 `6000.6.0f1`을 선택하고 기본 Editor 경로에 Windows Standalone Support와 WebGL Build Support를 설치한다.
5. Microsoft 공식 Visual Studio Community 설치 프로그램을 실행하고 기본 경로와 `Unity를 사용한 게임 개발` workload를 선택한다.
6. Hub와 Visual Studio 양쪽에서 같은 도구를 중복 설치하지 않는다. Hub가 이미 있으면 Visual Studio 선택 구성의 Unity Hub는 제외하고, Visual Studio가 이미 있으면 Hub의 Visual Studio 선택 구성은 제외한다.
7. 설치 후 이 문서의 실제 경로 확인과 `restore-session.ps1` 또는 Q0 수동 검사를 수행한다.
8. Hub와 Editor를 일반 사용자 권한으로 실행한다. 관리자 권한 경고가 반복되면 Hub를 알림 영역에서 완전히 종료하고 일반 실행으로 다시 시작한 뒤 프로젝트를 연다.

공식 다운로드 원본:

- Unity Hub: `https://unity.com/download`
- Unity Editor 6000.6.0f1: `https://unity.com/releases/editor/whats-new/6000.6.0f1`
- Visual Studio Community: `https://visualstudio.microsoft.com/downloads/`

설치 파일 자체는 Git에 보존하지 않는다. 초기화 뒤 위 공식 원본에서 다시 받고, 경로 또는 버전이 달라지면 실제 확인 뒤 이 문서와 검사 스크립트를 같은 Checkpoint에서 함께 갱신한다.

Hub 설치만으로 Editor가 즉시 자동 설치되는 것은 아니다. Hub 설치 뒤 Editor 버전과 모듈을 명시적으로 선택해야 하며, 프로젝트 생성은 Editor 설치와 별도 단계다.

관리형 PC에서 설치 프로그램이 차단되면 관리자 권한, 보안 설정 또는 실행 정책을 우회하지 않는다. 공식 설치 프로그램과 이미 허용된 GUI만 사용하고 실패 항목을 `[BLOCKED — 관리형 PC 정책]`으로 기록한다.

## Visual Studio

- Edition: `[확인 사실] Visual Studio Community 2026`
- 정확 버전: `[확인 사실] 18.10.12201.205`
- 설치 위치: `[확인 사실 — 해당 PC] C:\Program Files\Microsoft Visual Studio\18\Community`
- 필수 workload: `[확인 사실] Microsoft.VisualStudio.Workload.ManagedGame — Unity를 사용한 게임 개발`
- 필수 component: `[확인 사실] Microsoft.VisualStudio.Component.Unity — Visual Studio Tools for Unity`
- Installer 위치: `[확인 사실 — 해당 PC] C:\Program Files (x86)\Microsoft Visual Studio\Installer\setup.exe`

설치 완료 시 `Microsoft.VisualCpp.Redist.14` 경고가 발생했지만 Visual Studio 본체, Unity workload와 Unity component는 설치 등록됐다. `vcruntime140.dll`과 `msvcp140.dll`은 System32와 SysWOW64에 존재했다. DLL 존재만으로 runtime 정상 동작을 확정하지는 않는다. 이 경고만 보고 보안 정책을 우회하거나 반복 설치하지 않고, Visual Studio 실행 또는 Unity Compile이 실패할 때만 Microsoft 공식 Visual C++ Redistributable 복구/수정을 승인된 경로로 수행한다.

## Codex 및 로컬 설정

- 문서 / 기획 / 환경복구 / Git: GPT-5.6 Sol / Medium
- 단일 시스템: GPT-5.6 Sol / Medium~High
- 여러 전투 시스템 / AI / 경로 / 시뮬레이션 통합: GPT-5.6 Sol / High
- 기본 서브에이전트: 0
- 새 PC에서 Codex 프로젝트를 만들 때 정상 Clone한 `%USERPROFILE%\Iron-Trenches`만 작업 루트로 선택한다. `Downloads`의 ZIP 폴더에는 연결하지 않는다.
- GitHub App은 On-Demand 선택 도구이며 연결 시 계정과 대상 저장소 접근 가능 여부를 다시 확인한다.
- 앱 연결과 인증은 PC 초기화 뒤 유지된다고 가정하지 않는다.

`.codex/config.toml`은 PC별 경로, MCP 설정과 사용자 환경 정보를 포함할 수 있으므로 Git 정본으로 보존하지 않는다. 초기화 뒤 필요한 설정의 재등록법만 이 문서에 기록한다. Token, OAuth, Secret과 개인 인증정보는 기록하지 않는다.

## Unity MCP 복구 규격

- Unity 프로젝트가 생긴 뒤 Unity MCP 1개를 선택한다.
- 공식 Unity MCP가 당시 프로젝트 조건에서 사용 가능하면 우선 검토한다.
- 공식 MCP가 불가능하면 승인된 대체 Unity MCP 하나를 사용한다.
- Unity MCP 두 종류를 동시에 기본 활성화하지 않는다.
- 현재 Unity MCP: `[미구성]`; Editor Scene / Hierarchy / GameObject / Component / Console 실제 조회 Smoke 미실행

실제로 선택된 뒤 이 문서에 다음을 기록한다.

- MCP 이름과 정확 버전 또는 기준 commit
- 지원 Unity 버전
- 설치 위치와 등록 방법
- `.codex/config.toml` 재구성 방법
- Editor 측 설정과 서버 시작 방식
- 허용 tool과 금지 또는 승인 필요 tool
- Codex 재시작 필요 여부
- Smoke 호출과 정상 결과

설정 파일 존재만으로 연결 성공 처리하지 않는다. Unity Editor 실행, 실제 MCP 연결, 현재 Scene 조회, Console 조회, GameObject 또는 Component 조회까지 실제 호출한 뒤 복구 성공으로 판정한다.

## Project Skill 복구

- `.agents/skills/**`는 재현 가능한 프로젝트 작업 규칙이므로 생성된 뒤 Git 정본 후보다.
- `wwi-recovery-smoke`: `[미생성 — Compile / Play 기준선이 고정된 뒤 생성]`
- `wwi-fixed-battle-playtest`: `[미생성 / 보류 — GAME-DIRECTION-R1 이전 고정 전투 방향에 종속된 Skill은 만들지 않음]`
- Compile / Play / Reopen Smoke 전에는 환경 PASS를 전제하는 Recovery Skill을 생성하지 않는다.

## 정기 확인

가격이나 지원 조건은 영구 사실로 고정하지 않고 복구 또는 정기 확인 시 최신 조건을 확인한다.

| 확인 후보 | 마지막 확인일 | 현재 확인 결과 |
| --- | --- | --- |
| 현재 Codex 권장 모델 / 추론 강도 | `[미정]` | 복구 시 확인 |
| Unity MCP 지원 Unity 버전 | `[미정]` | DEV-BOOT-01 이후 확인 |
| Unity MCP 접근 조건 | `[미정]` | DEV-BOOT-01 이후 확인 |
| Unity 6.6 지원 상태와 최신 patch | `2026-09-09` | 프로젝트는 6000.6.0f1로 고정, 지원 상태는 복구 시 재확인 |
| Visual Studio Community 최신 지원 버전 | `2026-09-09` | 설치본 18.10.12201.205 |
| 실제 사용 중인 유료 서비스 비용 | `[사용 중인 서비스 없음]` | 사용을 시작한 경우에만 확인 |
| 필수 도구 지원 중단 여부 | `[미확인]` | 복구에 영향을 줄 때 확인 |

## Q0 Recovery Integrity

### Git / Source

- [ ] 다운로드 ZIP은 읽기 전용 안전본으로만 확인
- [ ] 올바른 Repository
- [ ] `%USERPROFILE%\Iron-Trenches`의 정상 working copy
- [ ] Codex 연결 root가 정상 Clone root와 일치
- [ ] 마지막 원격 Checkpoint 확인
- [ ] 예상하지 못한 로컬 변경 확인

### ManagedPc Source / Preview

- [ ] `global.json`의 .NET SDK 8.0.318 기준 확인
- [ ] `ManagedPcChecks` build 경고 0 / 오류 0
- [ ] 최신 assertion 전체 PASS
- [ ] `Preview/index.html` 직접 열기
- [ ] 현재 Bundle의 핵심 시나리오 조작과 기대 결과 확인
- [ ] Source-Only / Browser Preview / Unity Integration 판정 분리

Browser Preview의 사람 확인이 아직 없으면 `Browser Preview = PENDING`으로 둔다. 파일 존재나 정적 검사만으로 플레이 확인을 PASS 처리하지 않는다.

### Unity — DEV-BOOT 이후

- [ ] Unity Hub 존재
- [ ] 정확한 Unity 버전
- [ ] 필요한 Build Support 모듈
- [ ] Unity Hub와 Editor가 일반 사용자 권한으로 실행됨
- [ ] `Administrator Privileges Detected` 반복 경고 없음
- [ ] Project Open
- [ ] Package resolve
- [ ] Compile Error 없음
- [ ] Main Test Scene load
- [ ] 최소 Play Smoke

### Visual Studio — 사용 시

- [ ] Community 설치 존재
- [ ] `Unity를 사용한 게임 개발` workload
- [ ] Visual Studio Tools for Unity component
- [ ] Unity의 External Script Editor 연결

### MCP — 구성 이후

- [ ] 설정 존재
- [ ] 실제 연결
- [ ] Scene 조회
- [ ] Console 조회
- [ ] 필요한 최소 상태 조회

### Project Skill — 생성 이후

- [ ] 필요한 Skill 폴더 존재
- [ ] Skill 발견 가능
- [ ] Recovery Skill 실행 가능

Q0는 기계적 복구 무결성만 판단하며 사람의 재미를 판정하지 않는다.

## 초기화 PC 복구 순서

```text
허용된 Git Clone 경로 확인
↓
GitHub / Repository 접근 확인
↓
Downloads의 ZIP을 읽기 전용 안전본으로 확인
↓
%USERPROFILE%\Iron-Trenches 존재 여부 확인
↓
없으면 그 경로에 Iron-Trenches Clone, 있으면 기존 working copy 무결성 확인
↓
ZIP 전용 변경을 검토해 필요한 파일만 Clone에 반영
↓
Codex 연결 폴더를 Clone root로 변경
↓
AGENTS.md
↓
docs/00_시작.md
↓
docs/01_환경복구.md
↓
docs/40_구현순서.md
↓
필요한 관련 정본만 읽기
↓
허용되면 scripts/restore-session.ps1 -Mode ManagedPc 검사
↓
불가하면 이 문서의 Q0 수동 확인
↓
ManagedPcChecks 실행
↓
Preview/index.html을 브라우저로 열어 현재 시나리오 확인
↓
Source-Only / Browser Preview 판정 기록
↓
마지막 사람 승인 Result Bundle
↓
Commit / Push 뒤 다음 Result Bundle 재개
```

관리형 PC에서는 여기서 종료한다. 개인 PC로 전환할 때만 아래 `DEV-BOOT-RESUME-01`과 `UnityIntegration` 모드로 Unity 관련 단계를 추가한다.

## DEV-BOOT-RESUME-01 — 정상 PC 재개 순서

```text
정상 Git Clone / 승인된 working copy
↓
AGENTS.md → docs/00_시작.md → docs/01_환경복구.md → docs/40_구현순서.md
↓
Unity 6000.6.0f1과 Game/ 구조 확인
↓
Game/ Open → Package Resolve → Compile Error 0
↓
SampleScene 또는 향후 DevBootstrap Load
↓
Play → Bootstrap Smoke → Stop
↓
Editor 종료 → 재Open → 동일 Scene Smoke
↓
Unity MCP 하나만 연결
↓
Scene / Hierarchy / GameObject / Component / Console 조회
↓
wwi-recovery-smoke 생성 및 실제 검증
↓
Q0 READY
↓
RTS-CORE-01
```

`scripts/restore-session.ps1`은 Repository root와 Unity root를 구분하며 `Game/ProjectSettings`, `Game/Packages`와 `Game/Assets`를 읽기 전용으로 검사한다. 실행되지 않은 Compile / Play / MCP 검사를 PASS 처리하지 않는다.

PowerShell 실행 자체가 정책으로 차단되면 Execution Policy 변경, 다른 PowerShell binary, 관리자 실행 또는 AppLocker 우회를 시도하지 않는다. `[BLOCKED — 관리형 PC 정책]`으로 처리하고 같은 Q0 항목을 수동으로 확인한다. `restore-session.ps1`이 없어도 이 문서만으로 복구할 수 있어야 한다.

## 세션 종료 전 생존 확인

- `git status --short --branch`로 남은 변경을 확인한다.
- 현재 Result Bundle에 포함되는 파일만 명시적으로 stage한다.
- Secret과 개인 설정이 포함되지 않았는지 확인한다.
- 기술 검증과 필요한 사람 확인을 분리해 기록한다.
- 승인된 Checkpoint를 commit하고 `origin/main`에 push한다.
- local `HEAD`와 `origin/main`이 같은지 확인한다.
- 완료된 Checkpoint와 다음 재개 지점을 `40_구현순서.md`와 `지시장부.md`에 남긴다.

세션 종료 전에 push되지 않은 로컬 변경은 임시 작업으로 취급하며 복구를 보장하지 않는다.

## 보안

다음은 절대 저장하지 않는다.

- API Key
- OAuth Token
- Password
- 인증 Cookie
- 개인 인증 정보
- `.codex` 개인 설정
- 관리자 우회 방법
- 보안 정책 우회 절차
