# 구현 순서

이 문서는 현재 프로젝트 상태의 단일 Dashboard다. 과거 세션 이력을 누적하지 않는다.

## 완료된 Result Bundle

- `FOUNDATION-R1 — Recovery Architecture Alignment`
- `GAME-DIRECTION-R1 — Original RTS Direction Alignment`
- `DEV-BOOT-HANDOFF-01 — Managed PC Blocker Handoff`
- `RTS-CORE-01A — Engine-independent Command + Territory Domain`
- `RTS-CORE-01B — Capture + Frontline Domain`
- `DEV-WORKFLOW-R1 — Portable Source + Browser Preview Lane`
- `RTS-CORE-01C — Territory Graph + Strategic Anchors + Browser Playable Preview`
- `RTS-CORE-02A — Sector Income + Economy Domain + Playable Preview Hub`
- `RTS-CORE-02B — Sector-Scoped Free Construction Placement + Playable Preview`
- `RTS-CORE-02C — Construction Completion + General Building Territory Ownership + Playable Preview`
- `RTS-CORE-03A — Strategic Supply Connectivity Domain + Rule Preview`
- `RTS-PLAYTEST-03A-R1 — Terrain + Road + Physical Reinforcement Interdiction Microgame`
- `RTS-CORE-03B — Terrain Movement + Road Network + Physical Reinforcement Domain`
- `RTS-CORE-03C — Production + Reinforcement Source Integration`
- `RTS-CORE-03D — Route Threat + Interdiction + Isolation Semantics`

GAME-DIRECTION-R1은 문서 정렬과 사람 방향 확인이 완료됐다. 전체 RTS 방향과 `[대체됨]` Tactical 방향 상태를 유지한다.

## DEV-BOOT-HANDOFF-01

상태: `HANDOFF COMPLETE / DEV-BOOT BLOCKED`

- Q0: `PARTIAL`
- 관리형 PC Blocker 기록: 완료
- 보안 우회: 없음
- Unity Editor / Unity Hub 종료: 완료
- Unity MCP: 미구성
- `wwi-recovery-smoke`: 미생성
- 정상 PC 재개 Bundle: `DEV-BOOT-RESUME-01`

`cmd.exe` 직접 실행은 Access Denied이며 Unity Bee / ApiUpdater는 같은 프로세스 생성 단계에서 `GetLastError 5`로 실패한다. Safe Mode 탈출 자체는 가능하지만 Compile Error 0과 Play를 통과하지 않았다. C# `error CS`가 원인이라는 근거는 없다.

## 관리형 PC 개발 Lane

관리형 PC에서는 이 Lane을 매 세션 기본값으로 사용한다. 개인 PC도 같은 원격 Checkpoint와 Source 검증부터 시작한 뒤 Unity 통합 검증을 추가한다.

- 필수 복구: Git Clone/Pull, Codex, 승인된 Git 경로, 브라우저
- Source 검증: `global.json`의 .NET SDK 8.0.318 / `tools/ManagedPcChecks`
- 사람 검증: `previews/index.html` Hub → 현재 `RTS_CORE_03D_RouteThreatInterdiction.html`
- Harness: `tools/ManagedPcChecks/ManagedPcChecks.csproj`
- 대상: `Game/Assets/Scripts/Core/**/*.cs`
- 판정 분리: `Source-Only` / `Browser Preview` / `Unity Integration`
- 관리형 PC 복구 명령: `scripts/restore-session.ps1 -Mode ManagedPc -Online`
- 복구 검증: 고정 SDK 확인 뒤 ManagedPcChecks Release build/run과 현재 Preview Hub 존재를 자동 확인
- 개인 PC 전환 명령: `scripts/restore-session.ps1 -Mode UnityIntegration -Online`
- Unity Hub / Editor / Visual Studio 재설치: 관리형 PC의 매 세션 필수조건에서 제외
- 금지: `cmd.exe` 대체·우회, Unity Compile PASS 추정, Scene / Prefab / Component 통합 완료 추정

## DEV-WORKFLOW-R1 결과

- 복구 스크립트의 기본 모드를 `ManagedPc`로 고정하고 Unity 설치 검사를 SKIP으로 분리했다.
- 개인 PC용 `UnityIntegration` 모드는 같은 Source Checkpoint를 먼저 확인한 뒤 Unity 6000.6.0f1 통합 검사를 추가한다.
- Source SDK는 `global.json`에서 8.0.318로 고정했다.
- `previews/index.html`에 RTS-CORE-01B의 3-Sector 점령/전선 전환을 외부 의존성 없이 구성했다.
- 기술 정본은 순수 C# Core와 ManagedPcChecks이며 Browser Preview는 사람 플레이 확인용 표현 계층이다.
- Unity 생성물, IDE 상태, 인증정보는 환경 사이에 이관하지 않는다.
- Browser Preview 정적 시나리오 검증: PASS; 로컬 파일의 사람 브라우저 확인: PENDING

## RTS-CORE-01A 결과

목표: Unity와 분리된 최소 Command + Territory domain이 현재 PC에서 컴파일되고 기본 불변조건을 지킨다.

- `CommandOrder`: Squad id, Move / AttackMove, X/Z 목적지
- `SectorState`: Sector id, Owner id와 명시적 소유권 이전
- 직접 .NET Release build: PASS — 경고 0 / 오류 0
- ManagedPcChecks: PASS — 7 assertions
- Unity Compile / Scene / Play: DEFERRED
- 원격 보존: `main`의 Managed PC Source-Only Development Checkpoint

구현 수치, Capture 진행, 건물 소유권 이전, Frontline과 Unity adapter는 아직 포함하지 않았다.

## RTS-CORE-01B 결과

목표: 완료된 점령이 Sector 소유권을 바꾸고, 현재 소유권과 인접 관계에서 전선을 중복 없이 파생한다.

- `SectorTopology`: Sector 등록, 중복 없는 양방향 인접 관계, 인접/이웃 조회, 자기 연결 거부
- `SectorCapture` / `CaptureResult`: 점령 완료 적용, 이전/신규 소유자와 실제 변경 여부
- `FrontlineEdge` / `FrontlineResolver`: 방향 없는 경계 식별과 현재 상태 기반 전선 파생
- 3-Sector smoke: Blue West — Red Center — Red East에서 West-Center만 전선, Center의 Blue 점령 뒤 Center-East만 전선
- 직접 .NET Release build: PASS — 경고 0 / 오류 0
- ManagedPcChecks: PASS — 36 assertions; RTS-CORE-01A의 기존 7 assertions 포함
- Unity API 의존성: 없음
- Unity Compile / Scene / Play: DEFERRED

점령 시간·속도·범위·점령력·경합·감쇠·점수와 건물 이전/재가동은 포함하지 않았다. Q-007과 Q-010은 계속 `[미정]`이다.

## RTS-CORE-01C 결과

`RTS-CORE-01C — Territory Graph + Strategic Anchors + Browser Playable Preview`

### 기술 결과

- `SectorControlAnchor`: Anchor ID와 연결 Sector ID; Owner 중복 저장 없음
- `TerritoryGraph`: Topology/State/Anchor 연결, Sector↔Anchor 조회, Anchor 점령 완료, 현재 Frontline 파생
- 등록된 각 Sector의 Control Anchor 정확히 1개, Anchor ID 유일성, 미등록 입력 방어
- 3-Sector 통합: Anchor-C 점령 뒤 West-Center 전선이 Center-East로 이동
- 5-Sector fixture: Center-East 점령 뒤 해당 전선만 제거되고 Center-North/Center-South 유지
- 직접 .NET Release build: PASS — 경고 0 / 오류 0
- ManagedPcChecks: PASS — 73 assertions; 01A/01B의 기존 36 assertions 포함
- Preview 생성/정적 시나리오 Smoke: PASS — Anchor 선택, 점령, 색/전선/Event Log 갱신, Reset
- 외부 의존성/redirect: 없음
- Browser automated smoke: DEFERRED — Codex 브라우저의 로컬 `file://` 정책
- Human Playable Preview: PASS — 사용자 직접 확인 및 다음 진행 승인
- Unity Integration: DEFERRED

### 사람 확인 대상

사용자가 Territory/Anchor Preview와 index 처리 방향을 직접 확인하고 02A 진행을 승인했다.

Q-007과 Q-010은 계속 `[미정]`이며 점령 진행 수치, 건물, 경제, 보급, 수도와 Unity 기능은 01C에 포함하지 않았다.

## RTS-CORE-02A 결과

`RTS-CORE-02A — Sector Income + Economy Domain + Playable Preview Hub`

### 기술 결과

- `SectorIncomeProfile`: Sector ID와 0 이상의 단일 `[시험값]` Income
- `EconomyState`: Faction ID와 0 이상의 Balance; 02B에서 안전한 지출 API로 확장
- `SectorIncomeResolver`: 모든 Territory Sector의 정확히 1개 Profile과 현재 Owner에서 진영 수입 파생
- `SectorIncomeCollector` / `IncomeCollectionResult`: Timer 없는 명시적 1회 적립과 이전/신규 Balance
- 5-Sector 통합: Blue +65 회수 → Center 점령 → Frontline 이동/다음 수입 +125 → Balance 190
- checked 정수 계산: Balance overflow를 조용히 wrap하지 않음
- 직접 .NET Release build: PASS — 경고 0 / 오류 0
- ManagedPcChecks: PASS — 111 assertions; 01A/01B/01C의 기존 73 assertions 포함
- `previews/index.html`: 기존 3-Sector 구현을 제거한 Preview Hub, 상대 링크만 사용
- `previews/RTS_CORE_02A_Economy.html`: 정적 시나리오 Smoke PASS; Timer/외부 의존성/redirect 없음
- Browser automated smoke: DEFERRED — Codex 브라우저의 로컬 `file://` 정책
- Human Playable Preview: PASS — 사용자 직접 확인 및 다음 진행 승인
- Unity Integration: DEFERRED

### 사람 확인 대상

사용자가 02A Economy Preview를 직접 확인하고 02B 진행을 승인했다.

Q-004와 Q-008은 계속 열려 있다. 단일 Income/Balance, 40/60/50/25/35는 Prototype 시험 구조이며 최종 국가 경제 모델·자원·지급 주기를 확정하지 않는다.

## RTS-CORE-02B 결과

`RTS-CORE-02B — Sector-Scoped Free Construction Placement + Playable Preview`

### 기술 결과

- `BuildingDefinition`: Building Type, 0 이상 Cost, 유한한 양수 Footprint Radius
- `BuildingPlacementRequest`: Site/Builder/Target Sector/Building Type과 자유 `WorldPoint`; UI 선택 Sector를 명시하되 실제 위치 판정을 대체하지 않음
- `ConstructionSite`: 검증된 Type, Owner, 정확히 하나의 Sector, 자유 Position과 Footprint 보존
- `ISectorPlacementAreaResolver`: Point의 실제 Sector와 전체 Footprint 포함 여부를 Unity 비의존 계약으로 분리
- `BuildingPlacementService` / `BuildingPlacementResult`: Sector 존재·아군 소유·유한 위치·실제 Sector·전체 Footprint·충돌·자금을 검증하고 성공 시에만 지출 및 Site 추가
- `EconomyState`: 0 이상 금액의 `CanAfford` / `Spend`; 자금 부족과 모든 배치 실패에서 Balance 불변
- 고정 Build Slot/자동 위치 선택 없음; 동일 West Sector의 서로 다른 임의 좌표에 Barracks와 Depot 배치 PASS
- 5-Sector 통합: Blue +65 회수 → West Barracks로 65→15 → 실패 Atomicity → 추가 회수와 두 번째 West Site → Center 점령 → Income +125 → Center Site 배치
- 직접 .NET Release build: PASS — 경고 0 / 오류 0
- ManagedPcChecks: PASS — 267 assertions; 01A~02A의 기존 111 assertions 포함
- `previews/RTS_CORE_02B_FreeConstruction.html`: Sector 선택, Building 선택, Green/Red Ghost와 Footprint, 자유 배치, Site 소속, Capture/Income/Frontline/Event Log/Reset 정적 시나리오 PASS
- 02B Preview와 Hub의 사용자 화면 문구는 한글 우선으로 정리하고 내부 코드 식별자만 영어로 유지
- `previews/index.html`: 게임 로직 없이 현재 02B와 완료 02A/01C 상대 링크 제공
- Browser automated smoke: DEFERRED — Codex 브라우저의 로컬 `file://` 정책
- Human Playable Preview: PASS — 사용자 직접 체크리스트 확인 및 다음 진행 승인
- Unity Integration: DEFERRED

### 사람 확인 대상

사용자가 02B 자유 건설 Preview 체크리스트를 직접 확인하고 02C 진행을 승인했다.

사각형 Sector와 원형 Footprint, Barracks 50/18과 Depot 35/14는 `[시험 구현/시험값]`이다. Q-007 고립 방어시설과 Q-009 보급 연결의 건설 필수 여부는 계속 `[미정]`이다.

## RTS-CORE-02C 결과

`RTS-CORE-02C — Construction Completion + General Building Territory Ownership + Playable Preview`

### 기술 결과

- `BuildingState`: 완공 일반 건물의 Building/Type/Owner/Sector ID, Position과 Footprint; Owner 이전 시 Sector와 공간 정보 불변
- `ConstructionCompletionService` / `ConstructionCompletionResult`: Timer 없는 명시적 완공, Site 제거와 Building 추가, 새 BuildingId와 모든 Site 공간/소유 정보 보존
- 완공 실패: 잘못된/없는 Site ID, 잘못된/중복 Building ID를 구분하고 Site/Building 목록 불변
- `TerritoryBuildingOwnershipService` / `SectorBuildingTransferResult`: Building 목록 사전 검증 뒤 기존 Anchor Capture를 실행하고, 실제 점령 변경 시 해당 Sector의 완공 일반 Building만 이전
- 같은 Sector의 복수 일반 Building 이전, 이미 새 Owner인 Building 비집계, 다른 Sector Building 불변, 같은 Owner Capture 무변경
- 5-Sector 통합: Blue 수입 +65·West 자유 Site→Building 완공, Center 점령 뒤 Frontline 이동·Blue Income +125·Center 일반 Building Red→Blue·East Building Red 유지
- 직접 .NET Release build: PASS — 경고 0 / 오류 0
- ManagedPcChecks: PASS — 353 assertions; 01A~02B의 기존 267 assertions 포함
- `previews/RTS_CORE_02C_ConstructionOwnership.html`: 한글 UI에서 Site 배치→명시적 완공, Building Owner/Sector 상세, Center 점령·일반 Building 이전·East 불변·Frontline/Income/Reset 정적 시나리오 PASS
- `previews/index.html`: 게임 로직 없이 현재 02C와 완료 02B/02A/01C 상대 링크 제공
- Browser automated smoke: DEFERRED — Codex 브라우저의 로컬 `file://` 정책
- Human Playable Preview: PASS — 사용자 직접 체크리스트 확인 및 다음 진행 승인
- Unity Integration: DEFERRED

### 사람 확인 대상

사용자가 02C 건설 완공/일반 건물 소유권 Preview 체크리스트를 직접 확인하고 03A 진행을 승인했다.

02C의 자동 이전은 완공된 일반 Building에만 적용한다. 적 Unit과 Trench는 제외하며, Defense Facility는 Q-007, 미완공 ConstructionSite 함락은 Q-012로 계속 `[미정]`이다. 건설 Timer/진행, Worker, 건물 재가동 시간, 생산, HP/파괴/수리, 보급 조건과 Unity 통합은 포함하지 않았다.

## 현재 Result Bundle

`RTS-CORE-03E — Production Facility Capture + Queue Resolution`

### 03A 기준선과 사람 판정

- RTS-CORE-03A Source-Only: `PASS`
- Browser Rule Preview Static: `PASS`
- 03A Human Rule Readability: `PASS`
- 03A Human Supply Connectivity Understanding: `PASS`
- 03A Human RTS Strategy/Fun Fit: `PASS — R1 사람 Gameplay Validation`
- Unity Integration: `DEFERRED`
- RTS-CORE-03B Source-Only: `PASS`
- RTS-CORE-03B Browser Static: `PASS`
- RTS-CORE-03B Human Technical Preview: `PENDING`

기존 `RTS_CORE_03A_SupplyConnectivity.html`은 Supply Source, 보급 연결/완전 고립, Supply Line과 Frontline 구분, 단일 경로 상실 뒤 우회 유지, 모든 전략 경로 상실 뒤 고립, 소유권 유지, Building의 Sector 상태 공유와 연결 복구를 읽는 규칙 Preview다. 규칙 가독성과 연결 이해는 이 Preview에서 PASS했고, 실제 RTS 전략/재미는 이후 R1 Microgame 사람 Gameplay Validation에서 PASS했다.

### `[대체됨 — R1]` 이전 Microgame

- `previews/RTS_PLAYTEST_03A_SupplyStrategy.html`: 외부 의존성·서버·redirect 없는 독립 Browser RTS Microgame
- 전장: Blue Rear / Blue Approach / North Route / South Route / Red Front / Red Rear Supply
- 시작 병력 `[시험값]`: Blue 5개 Squad, North/South Guard 각 1개, Red Front Defender 3개
- 조작: 클릭 선택, Drag Selection, Shift 추가 선택, 우클릭 이동, A/버튼 Attack Move, 명령 목적지 피드백
- 전투: Squad HP, 사거리 내 단순 자동 공격, 0 HP 제거; Cover/Suppression/Morale/Ammo는 포함하지 않음
- 점령 `[시험 규칙/시험값]`: North/South/Front Control Anchor에서 Blue 존재·Red 부재 시 진행
- Red Supply: Red Rear→North→Front와 Red Rear→South→Front의 두 경로
- 한 경로 차단: 다른 우회로로 Red Front `SUPPLIED`, Reinforcement 계속
- 두 경로 차단: Red Front Owner와 기존 병력 능력은 유지한 채 `CUT OFF`, 새 Reinforcement만 중단
- Supply 복구: North 또는 South가 Red 통제로 돌아가면 경로와 Reinforcement 재개 가능
- Red AI: Route Guard는 자기 Anchor, Front Defender와 Reinforcement는 Front 방어
- 승리/패배: Red Front Anchor 점령 / Blue Squad 전멸; Reset과 Pause 제공
- 결과 통계: 시간, 양측 손실, 적 증원 수, Cut Off 경험/누적 시간, North/South 점령 여부
- 정적 Smoke: 초기화, 양측 생성, Click/Drag/Shift Selection, Move/Attack Move, Combat, 두 Route Capture, Supply Resolve/Cut Off/Restore, Reinforcement Active/Blocked, Win/Lose/Reset와 결과 통계 PASS
- 이 이전판은 비교용으로 보존하되 Hub의 CURRENT에서 R1으로 대체했다.

`Cut Off = Red Front Reinforcement 중단`은 이 Browser Playtest만의 `[시험 규칙]`이다. Supply Capacity/Resource, Ammo/Fuel, 생산·전투·경제 패널티와 최종 Reinforcement 규칙을 C# Core 또는 `docs/14_규칙_보급.md`에 확정하지 않았다. Q-007, Q-009와 Q-012도 계속 `[미정]`이다.

### R1 기술 결과

- `previews/RTS_PLAYTEST_03A_R1_TerrainRoadInterdiction.html`: 외부 의존성·서버·redirect 없는 독립 Gameplay Validation Microgame
- Terrain `[시험값]`: Road 1.40x, Open 1.00x, Rocky 0.80x, Forest 0.70x, Mud 0.60x
- Forest는 탐지 거리 0.70x, Rocky는 받는 피해 0.85x의 작은 `[시험 효과]`; 정식 LOS/Cover/Concealment가 아님
- Road는 실제 Movement Infrastructure이며 자체 Cover/Concealment 없음
- 자유 Road Construction: 버튼 → 아군 시작점 → 아군 끝점 → Preview/Validation → 확정; 고정 Slot/Node 없음
- Browser `RoadSegment`: ID, Start/End, Owner, Width와 여러 `TraversedSectorIds`; 일반 Building의 단일 Sector 규칙으로 취급하지 않음
- Road 건설은 즉시·무료 `[시험 규칙]`; 적 소유 지역 시작/종료/관통과 최소 길이 미달은 거부
- Player Squad는 Road waypoint에 묶이지 않고 전장 어느 위치로도 Offroad 이동 가능하며 현재 위치 Terrain 배율 적용
- Red Reinforcement는 Rear에서 실제 Squad로 Spawn해 Red 소유 North/South Road waypoint를 따라 Front로 이동
- Front 도착 전에 Blue Squad가 발견·공격·파괴할 수 있으며 Spawned / Reached Front / Destroyed En Route 통계를 분리
- Strategic Connection이 남은 상태에서 이동 증원이 공격받으면 `INTERDICTED`; Sector Owner와 Strategic Cut Off는 자동 변경하지 않음
- North 또는 South만 점령하면 다른 경로로 Connected 유지·이동 증원 경로 변경, 둘 다 점령하면 `CUT OFF`와 새 Dispatch 중단
- Cut Off 전에 출발한 증원은 삭제·Teleport하지 않고, 남은 Red 경로로 변경하거나 경로가 없으면 현 위치 방어
- 점령한 North/South Corridor로 Blue Road를 연장해 분대 재배치 속도 차이를 시험 가능
- 결과 통계: 시간, 양측 손실, Reinforcement Spawn/Front 도착/이동 중 격파, Interdiction/Cut Off, Corridor 점령, Road/Offroad 거리, Road 수, Forest/Rocky 체류 시간
- Static Smoke: 초기화, Terrain/Road, 자유 Road 건설·검증, 이동 배율/시험 효과, 선택/이동/Attack Move, Rear Spawn·실제 이동·Road 우선·Front 도착, 요격/이동 중 격파, Corridor 점령/우회/Cut Off/복구 가능 상태, Pause/Reset/통계 PASS
- `previews/index.html`: 03E 현재 핵심 시스템, 03D/03C/03B 이전 핵심 시스템, R1 게임플레이 검증 완료와 03A 이하 기술 기록을 상대 링크로 분리
- Browser automated smoke: `DEFERRED — Codex 브라우저의 로컬 file:// 정책`
- 정확한 standalone .NET SDK 8.0.318 확보 후 03B 이전 baseline Release build 경고 0 / 오류 0, ManagedPcChecks 435 assertions PASS
- RTS-PLAYTEST-03A-R1 Browser Static: `PASS`
- RTS-PLAYTEST-03A-R1 Human: `PASS`
- RTS-PLAYTEST-03A-R1 Technical Regression: `PASS`
- R1 checkpoint를 `main`에 fast-forward 승격 완료

사람 Gameplay Validation 결과 Terrain + Road + Physical Reinforcement + Interdiction 구조가 Iron & Trenches RTS 방향에 적합하다고 확인됐다. 다만 Road 1.40x, Forest 0.70x, Rocky 0.80x, Mud 0.60x와 Forest 탐지·Rocky 피해 감소는 모두 `[시험값]`이며 최종 Balance 또는 Combat Rule이 아니다.

Terrain/Road/Reinforcement의 C# 최종 API와 수치는 확정하지 않았다. Road의 이동 보너스와 자체 Cover/Concealment 없음은 `[현재 방향]`이며, 세부 수치·건설 비용/시간·파괴/수리·소유권과 보병/차량 차이는 Q-013 `[미정]`이다. 참호/방어시설을 이용한 차단 지점 고착은 향후 전선 전투 후보이며 R1에 구현하지 않았다.

### R1 사람 Playtest 완료

R1에서 아래 네 가지 판을 비교했고 사람 판정은 `PASS`다.

1. Road/Open 중심으로 Front 정면 공격한다.
2. Forest/Rocky로 우회해 Front가 Connected인 동안 이동 중 Reinforcement를 요격한다.
3. North/South Corridor를 점령해 Strategic Cut Off를 만든 뒤 Front를 공격한다.
4. 선택적으로 점령 지역에 Blue Road를 연장해 재배치 속도를 확인한다.

- `PASS`: Terrain + Road + Physical Reinforcement + Interdiction 구조가 RTS 방향에 맞으면 다음 Core 후보 진행 가능
- `ADJUST`: Road/Terrain 배율, Road UX, 증원 빈도, 맵과 요격 효과를 작은 후속 Playtest에서 조정
- `FAIL`: 03B를 HOLD하고 복잡도와 전투 흐름을 다시 설계

사람 판정은 `PASS`다. 이 판정은 구조와 플레이 방향에 대한 승인이고 시험 배율·전투 효과의 최종 확정은 아니다.

### 03B 기술 결과

- Slice A: `TerrainMovementProfile`과 `ITerrainMovementResolver`로 WorldPoint의 이동 배율을 Unity와 분리했다. Core 범위는 이동뿐이며 Cover/Concealment/Detection/Damage는 포함하지 않는다.
- Slice B: 자유 endpoint, 유한한 Width와 여러 `TraversedSectorIds`를 가진 `RoadSegment`를 Building과 분리했다. 고정 Road Slot은 없고 endpoint Junction은 동적으로 파생한다.
- `RoadPlacementService`는 통과 Sector 존재와 전부 Builder 소유 여부를 검증하며 실패 시 `RoadNetwork`를 바꾸지 않는다. 비용·시간·소유권 이전은 Q-013 `[미정]`이다.
- `RoadNetwork`는 ID 유일성, 폭 안 Point 판정, 연결 Segment와 최소 거리 endpoint 경로를 제공한다.
- `MovementSpeedResolver`는 Road 위에서 Road Profile만, 밖에서는 underlying Terrain Profile만 사용한다. R1 배율은 ManagedPcChecks/Preview의 `[시험값]`이다.
- Slice C: Supplied source/destination만 `ReinforcementDispatchService`가 허용하고 Road가 연결되면 Road Route, 없으면 Offroad Route를 만든다.
- `ReinforcementTransit`은 호출자가 전달한 deltaSeconds로 `EnRoute → Arrived`를 실제 진행한다. `DestroyedEnRoute` 이후에는 이동·도착하지 않는다.
- Physical Interdiction은 Transit 손실로 표현하고 `StrategicSupplyStatus`에 Interdicted를 추가하지 않았다.
- Strategic Cut Off는 새 Dispatch를 차단하지만 이미 EnRoute인 객체를 자동 삭제·Teleport하지 않는다. 동적 재탐색/후퇴/대기는 Q-014 `[미정]`이다.
- 최종 Release build: 경고 0 / 오류 0. ManagedPcChecks: `537 assertions PASS`.
- `previews/RTS_CORE_03B_TerrainRoadReinforcement.html` Static Smoke: Terrain/Road 렌더 데이터, Road/Offroad 이동, Road 우선/Offroad fallback, EnRoute/Arrived/DestroyedEnRoute, Interdiction/CutOff 분리, 신규 Dispatch 차단, 기존 Transit 유지, 복구/Reset `13 checks PASS`.

### 03B 사람 Technical Preview 대상

`previews/index.html`의 CURRENT CORE PREVIEW에서 03B를 연다. Terrain별 속도, Road overlay, Offroad fallback, Rear Dispatch, EnRoute/Arrival, DestroyedEnRoute, Corridor Cut/Restore와 기존 EnRoute 유지가 C# 계약을 명확히 설명하는지 확인한다. 자동 Static PASS만으로 사람 판정을 올리지 않는다.

03B Human Technical Preview는 `PENDING`을 유지한다. 사용자는 이 사람 판정을 자동 승격하지 않고 03C 진행을 별도로 승인했다.

### 03C 기술 결과

- `ProductionDefinition` / `ProductionFacilityProfile`: 단일 시험 자원 비용·생산 시간·기본 이동 속도와 시설별 생산 가능 Unit Type을 Unity API 없이 정의한다.
- `ProductionService`는 완공 시설, 요청 Owner, 생산 가능 품목, 목적지, 자금과 중복 ID를 모두 검증한 뒤에만 기존 `EconomyState` 지출과 시설별 Queue 등록을 원자적으로 적용한다.
- `ProductionQueue`는 FIFO 순서를 보존하고 완료 뒤에도 Order/Reinforcement ID 재사용을 거부한다.
- `ProductionAdvanceService`는 명시적 deltaSeconds와 초과 시간을 소비하고, 완료 시 전선이 아니라 실제 `BuildingState.Position/SectorId`의 `ReadyAtSource`를 만든다.
- 생산 중 시설 Owner가 바뀌면 Queue를 이전·삭제하지 않고 `OwnershipConflict`로 안전하게 정지한다. 최종 정책은 Q-015 `[미정]`이다.
- `ProductionReinforcementIntegrationService`는 Ready 객체를 기존 03B Dispatch에 연결한다. Road 연결 시 Road, 없으면 Offroad, 성공 뒤 `EnRoute → Arrived`, 이동 중 파괴는 `DestroyedEnRoute`다.
- 03C 시험에서는 기술 `CutOff`, 즉 완전 고립 중 생산이 계속되지만 Dispatch는 차단되고 Ready 객체와 ID는 보존된다. 복구 뒤 같은 Ready 객체를 파견한다. 생산 자체의 보급 효과는 Q-016 `[미정]`이다.
- 생산 완료→Ready→Dispatch가 분리되어 전선 즉시 생성과 Teleport가 없다.
- 최종 .NET SDK 8.0.318 Release build: 경고 0 / 오류 0.
- ManagedPcChecks: 기존 537개를 포함한 `658 assertions PASS`.
- `previews/RTS_CORE_03C_ProductionReinforcement.html` 정적 Smoke: 생산 요청/비용, 부분 진행, FIFO 완료, 시설 위치 Ready, Road/Offroad, EnRoute/Arrived/DestroyedEnRoute, 완전 고립 파견 차단·Ready 보존·같은 ID 복구와 Reset `13 checks PASS`.
- HTML 사용자 인터페이스는 한국어 우선 규칙을 적용하며 외부 의존성·서버·redirect가 없다.

### 03C 판정과 다음

- RTS-CORE-03C Source-Only: `PASS`
- RTS-CORE-03C Browser Static: `PASS`
- RTS-CORE-03C Human Technical Preview: `PASS — 사용자 체크리스트 확인`
- RTS-CORE-03B Human Technical Preview: `PENDING — 별도 보존`
- Unity Integration: `DEFERRED`; DEV-BOOT: `BLOCKED`; Q0: `PARTIAL`
- 사용자는 생산→출발 대기→실제 이동→도착, Road 우선·Offroad fallback, 이동 중 요격, 완전 고립 중 Ready 보존과 같은 ID 복구, 한글 UI를 확인했다.

### 03D 기술 결과

- `RouteThreatSource` / `RouteThreatKind`: 향후 실제 Unit/Outpost/Trench가 제공할 수 있는 이동형·고착형 위협 입력을 순수 C# 계약으로 추가했다.
- `RouteThreatResolver`는 적대 Source에서 각 Route Leg까지의 최소 거리를 평가하고 반경 안 위협을 한 번씩 합산한다. 아군 Source는 무시한다.
- `RouteThreatAssessment`는 누적 위협, Source 수와 고착형 존재 여부를 Dispatch 시점 불변 Snapshot으로 보존한다.
- `RoutePlanningProfile`은 이동 시간과 위협 가중치를 주입한다. 정확한 반경·가중치·Score 비율은 Q-017 `[미정/시험값]`이다.
- `RoadNetwork.FindPaths`가 기존 endpoint graph에서 단순 후보 Road 경로를 파생하며, 기존 `TryFindPath`도 같은 후보 결과의 최단 경로를 사용한다.
- `ReinforcementRoutePlanner`는 후보 Road 경로의 이동 시간+위협 Score를 비교하고 Road가 없으면 기존 Offroad fallback을 유지한다.
- 약한 위협은 빠른 경로를 무조건 배제하지 않으며, 안전한 우회가 유리하면 우회하고, 위협 또는 고착 위협이 있는 길만 남아도 Dispatch한다.
- `ReinforcementDispatchService`와 03C `ProductionReinforcementIntegrationService`에 위협 인지 overload를 추가했다. 완전 고립만 신규 Dispatch를 막고 Ready를 보존한다.
- ThreatSource 추가는 Sector Owner와 Strategic Supply Snapshot을 바꾸지 않는다. `DestroyedEnRoute`도 전략 상태를 바꾸지 않는다.
- 이미 이동 중인 Transit은 위협 또는 전략 연결 변경으로 삭제·Teleport·자동 재탐색되지 않는다. 동적 처리 Q-014는 계속 `[미정]`이다.
- 실제 Combat, HP 피해, Squad/Outpost/Trench Core와 자동 위협 생성은 구현하지 않았다.
- .NET SDK 8.0.318 Release build: 경고 0 / 오류 0.
- ManagedPcChecks: 기존 658개를 포함한 `741 assertions PASS`.
- `previews/RTS_CORE_03D_RouteThreatInterdiction.html`: 한글 UI, 안전 경로·이동형 위협·안전 우회·위험/고착 경로 강행·완전 고립 차단·Ready/EnRoute 유지·복구·Reset 정적 Smoke `11 checks PASS`.

### 03D 판정과 다음

- RTS-CORE-03D Source-Only: `PASS`
- RTS-CORE-03D Browser Static: `PASS`
- RTS-CORE-03D Human Technical Preview: `PASS — 사용자 체크리스트 확인`
- Unity Integration: `DEFERRED`; DEV-BOOT: `BLOCKED`; Q0: `PARTIAL`
- 사용자는 적 병력·전초기지의 경로 위협, 안전 우회, 위험 경로 강행과 전략 연결 완전 단절을 구분하는 흐름을 확인했다.

### 03E 기술 결과

- 기존 `TerritoryBuildingOwnershipService`가 반환한 `SectorBuildingTransferResult`를 소비하며 Sector/Anchor/Building 점령 로직을 복제하지 않는다.
- `ProductionCaptureQueuePolicy.CancelWithoutRefund`로 Q-015 최종 규칙과 분리된 `[시험 정책]`을 명시했다.
- `ProductionFacilityCaptureResolutionService`는 점령 결과, 완공 Building, 생산시설 Profile과 시설별 Queue 연결을 모두 사전 검증한 뒤 대상 Queue를 결정적으로 비운다.
- 진행 중 Order와 모든 대기 Order를 함께 취소하고 진행도를 소실한다. EconomyState를 입력받지 않아 이전 소유 진영 환불과 새 소유 진영 무료 이전이 구조적으로 없다.
- 같은 Sector의 복수 생산시설 Queue는 독립적으로 정리하고, 비생산시설과 다른 Sector Queue는 변경하지 않는다. 빈 생산시설도 오류 없이 빈 Queue를 유지한다.
- 모순된 Capture Result나 Queue Owner 연결은 다른 Queue를 지우지 않고 fail-safe 결과를 반환한다.
- 점령 전에 완성된 `ReadyReinforcement`와 이미 `EnRoute`인 `ReinforcementTransit`은 자동 삭제·Owner 변경·Teleport하지 않는다.
- 기존 Source Ownership 검증으로 옛 진영 Ready의 파견은 거부되며 같은 객체를 보존한다. 최종 운명은 Q-018 `[미정]`이다.
- 새 Owner는 빈 Queue에서 기존 03C 생산 요청을 즉시 사용할 수 있고 정상 비용을 지불한다. 완료 뒤 03D 위협 인지 Dispatch까지 통합된다.
- Q-015는 03E 사람 확인 전까지 `[미정]`이며 점령 후 Repair/Stabilization 시간은 이번 범위에 포함하지 않았다.
- .NET SDK 8.0.318 Release build: 경고 0 / 오류 0.
- ManagedPcChecks: 기존 741개를 포함한 `845 assertions PASS`.
- `previews/RTS_CORE_03E_ProductionCaptureQueue.html`: 한글 UI, 점령·소유권 이전·진행/대기 작업 취소·환불 없음·Queue 이전 없음·Ready/EnRoute 유지·새 Owner 생산·Reset 정적 Smoke `15 checks PASS`.

### 03E 판정과 다음

- RTS-CORE-03E Source-Only: `PASS`
- RTS-CORE-03E Browser Static: `PASS`
- RTS-CORE-03E Human Technical Preview: `PENDING`
- Q-015: `[미정] — 현재 시험 정책의 사람 판정 대기`
- Q-018: `[미정] — 점령된 Source의 출발 대기 병력 최종 처리`
- Unity Integration: `DEFERRED`; DEV-BOOT: `BLOCKED`; Q0: `PARTIAL`
- 사람 판정 뒤 다음 후보 하나를 선택한다: `RTS-CORE-04A — Technology Research Foundation`, `FRONTLINE-CORE-01A — Squad Combat Foundation`, `DEFENSE-CORE-01A — Trench / Defensive Position Foundation`.

## 정상 PC 병행 재개 Bundle

`DEV-BOOT-RESUME-01 — Normal Environment Recovery`

Clone / Source → Unity 6000.6.0f1 → Package Resolve → 기존 Source-Only Core의 Unity Compile → Compile Error 0 → Dev Scene → Play / Stop → Reopen → Unity MCP 조회 → `wwi-recovery-smoke` → Q0 READY 순으로 닫는다.

권장 실행 설정: 현재 Source-Only Core는 GPT-5.6 Sol / High, Unity 통합 복구도 GPT-5.6 Sol / High / Subagents 0.
