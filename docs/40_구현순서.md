# 구현 순서

이 문서는 현재 프로젝트 상태의 단일 Dashboard다. 과거 세션 이력을 누적하지 않는다.

## 완료된 Result Bundle

`FOUNDATION-R1 — Recovery Architecture Alignment`

## 상태

- 기술 확인: 완료 — 정본 책임, 복구 절차, 검사 스크립트와 Git 제외 경계를 내부 검증함
- 사람 확인: 완료 — FOUNDATION-R1 Checkpoint 범위 승인
- 원격 보존: `main`의 FOUNDATION-R1 Checkpoint

## 목표

- `00_시작.md`를 최소 Router로 한정한다.
- `01_환경복구.md`를 실제 환경과 복구 절차의 단일 정본으로 정리한다.
- `restore-session.ps1`을 기계적 검사 전용으로 한정한다.
- 현재 작업 상태와 다음 행동은 이 문서만 소유한다.
- RPGpro의 게임 규칙이나 제작 규칙은 가져오지 않는다.

## 현재 Result Bundle

`DEV-BOOT-01 — Unity Development Baseline`

아직 시작하지 않았다.

## 마지막 원격 Checkpoint

- branch: `main`
- Checkpoint: `FOUNDATION-R1 — Recovery Architecture Alignment`
- 정확한 HEAD는 GitHub `main`의 최신 commit이 소유한다.

## 운영 제약

- 현재 작업 폴더는 `.git`이 없는 `Iron-Trenches-main` 임시 반입/안전본이다.
- 관리형 PC 정책으로 Git CLI 프로세스 실행이 `Access denied` / `EPERM`으로 차단됐다.
- Codex GitHub App 읽기는 가능하지만 쓰기는 403으로 거부됐다.
- 위 GitHub App 403은 DEV-BOOT의 디자인 blocker가 아니며 허용된 Git/GUI/원격 쓰기 경로로 대체한다.

## Blocker

현재 확인된 DEV-BOOT 디자인 blocker는 없다.

## 다음 행동

1. 정상 Clone 또는 승인된 원격 작업 환경을 준비한다.
2. `docs/01_환경복구.md`의 Q0 Git / Source 항목을 확인한다.
3. DEV-BOOT-01에서 실제 Unity 버전, Package, Main Test Scene과 복구 기준을 고정한다.

PC 초기화가 임박하면 새 개발보다 현재 로컬 변경의 원격 생존본 확보를 우선한다.

## 다음 구현 Bundle

`DEV-BOOT-01 — Unity Development Baseline`

권장 실행 설정: GPT-5.6 Sol / High / Subagents 0.
