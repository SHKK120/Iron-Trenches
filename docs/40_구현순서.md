# 구현 순서

이 문서는 현재 프로젝트 상태의 단일 Dashboard다. 과거 세션 이력을 누적하지 않는다.

## 완료된 Result Bundle

- `FOUNDATION-R1 — Recovery Architecture Alignment`
- `GAME-DIRECTION-R1 — Original RTS Direction Alignment` 기술 문서 정렬

## GAME-DIRECTION-R1 상태

- 기술 확인: 완료 — 관련 정본을 원래 Iron & Trenches의 전체 RTS 방향으로 재정렬함
- 사람 확인: 대기 — 현재 정체성과 새 RTS-CORE 순서를 사용자가 확인해야 함
- 게임 기능 구현: 시작하지 않음
- 원격 보존: `main`의 GAME-DIRECTION-R1 Checkpoint

## 현재 방향

`[현재 방향 — 사람 확인 대기]`

Unity 기반 Low-Poly 3D / 2.5D RTS 안에 Sector, 거점, 자동 지역 경제, 국가 경제 / 전쟁 수행 능력, 자유 건설, 생산, 기술 연구, RTS 보급선, 분대 / 소대 지휘, 전선 돌파와 수도 / 핵심 지역 승리가 한 경기 구조로 존재한다.

Cover, Trench, MG, LOS, Suppression, Morale과 Ammo는 이 전체 RTS의 전선 전투 하위 레이어다.

## DEV-BOOT 환경 상태

`DEV-BOOT — Development Environment Baseline`은 완료 처리하지 않는다.

- Repository / Clone: PASS
- 원격 Checkpoint 확인: PASS — `87b5482 DEV-BOOT-03: standardize Codex Git workflow`
- Unity Hub 43.5.0: PASS
- Unity Editor 6000.6.0f1: PASS
- Windows Standalone / WebGL Support: PASS
- Visual Studio Community 2026 18.10.12201.205, Unity workload/component와 `com.unity.ide.visualstudio` 2.0.26: PASS
- Unity External Script Editor 실제 선택값: 미확인
- Unity 프로젝트 `Game`, URP 17.6.0, Input System 1.20.0: PASS
- Main Dev Scene `Assets/Scenes/SampleScene.unity` Open / Load: PASS
- Missing Script / Missing Reference: 현재 Scene 파일과 로그에서 발견되지 않음
- Compile Error 0: BLOCKED — Bee가 `cmd.exe`를 통한 ApiUpdater 실행에서 Windows 오류 5로 실패하고 Safe Mode 진입
- Play / Bootstrap / 화면 / Stop / Reopen Smoke: 미실행
- Unity 일반 사용자 실행: FAIL — 실행 창 제목이 `Administrator:`로 표시됨
- Unity MCP: 미구성, 실제 조회 미실행
- `wwi-recovery-smoke`: 미생성 — Compile / Play 기준선 뒤 생성
- `wwi-fixed-battle-playtest`: 미생성 / 보류

판정: `DEV-BOOT Core BLOCKED / MCP 미구성`. MCP 하나 때문에 Core 전체를 실패 처리한 것이 아니라 Compile과 Play 기준 자체가 아직 충족되지 않았다.

## 다음 행동

1. 정책 제약이 없는 표준 사용자 환경 또는 시설 관리자 허용 아래 Unity Editor를 일반 사용자로 연다.
2. Compile Error 0 → SampleScene Load → Play → 화면 확인 → Stop → Editor 종료 / 재실행 Smoke를 실제 통과시킨다.
3. 현재 선택할 Unity MCP 하나만 최소 권한으로 연결하고 Scene / Hierarchy / GameObject / Component / Console 조회를 확인한다.
4. 실제 Baseline이 고정된 뒤 `.agents/skills/wwi-recovery-smoke/`를 생성한다.
5. GAME-DIRECTION-R1 사람 확인 뒤에만 `RTS-CORE-01`을 시작한다.

## 사람 확인 뒤 제안 구현 순서

### RTS-CORE-01 — Command + Territory Skeleton

RTS Camera, Selection, Squad, Move / Attack Move, Sector, Strongpoint, Capture, Ownership, Frontline과 Capital placeholder로 “부대를 움직여 Sector를 점령하고 전선이 이동한다”를 검증한다.

### RTS-CORE-02 — Economy + Free Construction

Sector Auto Income, 최소 Resource, 자유 Placement, Construction, Building Ownership / Capture Transfer와 Production Building으로 “점령 지역이 경제를 만들고 원하는 위치에 시설을 건설한다”를 검증한다.

### RTS-CORE-03 — Production + Supply

Production Queue, Reinforcement, Supply connectivity / Cut-off / status와 기본 AI 생산으로 “후방 생산과 보급망이 전선을 유지한다”를 검증한다.

### RTS-CORE-04 — Research + Complete War Loop

최소 Tech Research, Capital / Core Region, Victory / Defeat, Enemy strategic AI, Match restart와 Result summary로 한 경기의 경제 → 연구 → 생산 → 돌파 → 영토 → 수도 승리를 완결한다.

### 이후 전투 깊이

- `FRONTLINE-COMBAT-01`: Cover, Trench, MG, LOS, Fire
- `FRONTLINE-PRESSURE-01`: Suppression, Morale, Retreat, Ammo

## 다음 후보 Bundle

`RTS-CORE-01 — Command + Territory Skeleton`

GAME-DIRECTION-R1 사람 확인 전에는 시작하지 않는다.

권장 실행 설정: 문서 확인은 GPT-5.6 Sol / Medium, RTS-CORE 구현은 GPT-5.6 Sol / High / Subagents 0.
